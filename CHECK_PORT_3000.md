# 🔍 ポート3000の状態確認

## 現在の状況

`xdg-open`エラーは無視して大丈夫です（Viteがブラウザを開こうとして失敗しただけ）。

重要なのは、フロントエンドサーバーが実際にポート3000でリッスンしているかです。

---

## 確認手順

### ステップ1: フロントエンドサーバーがポート3000でリッスンしているか確認

VPSサーバーで以下を実行：

```bash
# ポート3000を使用しているプロセスを確認
sudo lsof -i :3000

# または
sudo netstat -tlnp | grep 3000
```

---

### ステップ2: フロントエンドサーバーの出力ログを確認

```bash
# エラーではなく、出力ログを確認
pm2 logs event-aeon-frontend --lines 50 --nostream | grep -v "xdg-open"

# または、すべてのログを見る
pm2 logs event-aeon-frontend --lines 50 --nostream
```

フロントエンドサーバーが起動している場合、以下のようなメッセージが見られるはずです：
```
VITE v5.x.x  ready in XXX ms
➜  Local:   http://localhost:3000/
➜  Network: http://0.0.0.0:3000/
```

---

### ステップ3: ローカルからフロントエンドにアクセスできるか確認

VPSサーバー上で：

```bash
# ローカルからアクセス
curl http://localhost:3000

# または、curlでヘッダーだけ確認
curl -I http://localhost:3000
```

---

### ステップ4: vite.config.tsの設定を確認

`vite.config.ts`の`open: true`設定を無効にして再起動：

```bash
cd ~/Event-AEON

# vite.config.tsを編集（必要に応じて）
# open: true を open: false に変更

# フロントエンドを再起動
pm2 restart event-aeon-frontend

# または、一度削除して再起動
pm2 delete event-aeon-frontend
pm2 start npm --name "event-aeon-frontend" -- run preview
pm2 save
```

---

## 完全な確認コマンド（一括実行）

```bash
# 1. PM2の状態
pm2 status

# 2. ポート3000の使用状況
sudo lsof -i :3000

# 3. フロントエンドのログ（xdg-openエラーを除く）
pm2 logs event-aeon-frontend --lines 30 --nostream | grep -E "(VITE|Local|Network|ready|error)" | grep -v "xdg-open"

# 4. ローカルからアクセステスト
curl -I http://localhost:3000

# 5. ファイアウォール状態
sudo ufw status
```

---

## もしポート3000がリッスンしていない場合

```bash
cd ~/Event-AEON

# ビルドが存在するか確認
ls -la dist/

# ビルドがない場合は再ビルド
npm run build

# フロントエンドを再起動
pm2 delete event-aeon-frontend
pm2 start npm --name "event-aeon-frontend" -- run preview
pm2 save

# ログを確認
pm2 logs event-aeon-frontend --lines 20
```
