# 🧹 フロントエンド重複プロセスを整理

## 問題

`event-aeon-frontend` が2つ起動しています（重複）。

---

## 解決方法

VPSサーバーで以下を実行：

```bash
# 重複しているフロントエンドプロセスを1つ削除
pm2 delete 2

# または、名前で削除（古い方）
pm2 delete event-aeon-frontend

# 再度起動（1つのみ）
cd ~/Event-AEON
pm2 start npm --name "event-aeon-frontend" -- run preview

# 設定を保存
pm2 save

# 状態を確認
pm2 status
```

---

## または、クリーンアップして再起動

```bash
# すべてのプロセスを停止
pm2 stop all

# フロントエンドプロセスをすべて削除
pm2 delete event-aeon-frontend

# バックエンドAPIはそのまま残す（正常に動作しているため）

# フロントエンドを1つだけ起動
cd ~/Event-AEON
pm2 start npm --name "event-aeon-frontend" -- run preview

# 設定を保存
pm2 save

# 状態を確認
pm2 status
```

---

## 期待される結果

```
┌────┬────────────────────────┬─────────┬─────────┬──────────┬────────┬──────┬───────────┐
│ id │ name                   │ status  │ restart │ uptime   │ cpu    │ mem  │ watching  │
├────┼────────────────────────┼─────────┼─────────┼──────────┼────────┼──────┼───────────┤
│ 0  │ event-aeon-api         │ online  │ 0       │ Xm       │ 0%     │ XXmb │ disabled  │
│ 1  │ event-aeon-frontend    │ online  │ 0       │ Xm       │ 0%     │ XXmb │ disabled  │
└────┴────────────────────────┴─────────┴─────────┴──────────┴────────┴──────┴───────────┘
```

フロントエンドが1つのみになれば完了です。
